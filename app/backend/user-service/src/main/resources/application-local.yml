spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:your-google-client-id}
            client-secret: ${GOOGLE_CLIENT_SECRET:your-google-client-secret}
            scope:
              - profile
              - email
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/auth
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://openidconnect.googleapis.com/v1/userinfo
            user-name-attribute: sub


#        registration:
#          canvas:
#            client-id: <Canvas Client ID>
#            client-secret: <Canvas Client Secret>
#            authorization-grant-type: authorization_code
#            redirect-uri: http://localhost:8081/login/oauth2/code/canvas
#            scope:
#              - url:GET|/api/v1/users/self/profile
#        provider:
#          canvas:
#            authorization-uri: https://<canvas-domain>/login/oauth2/auth
#            token-uri: https://<canvas-domain>/login/oauth2/token
#            user-info-uri: https://<canvas-domain>/api/v1/users/self/profile
#            user-name-attribute: id