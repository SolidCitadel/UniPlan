[project]
name = "uniplan-crawler"
version = "0.1.0"
description = "Multi-university course data crawler for UniPlan"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.8.0",
    "pytest>=8.0.0",
]
