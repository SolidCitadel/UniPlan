name: uniplan_e2e
description: End-to-end tests hitting UniPlan API Gateway
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.9.0

dependencies:
  http: ^1.2.2
  test: ^1.25.8
  dotenv: ^4.2.0
  uniplan_models:
    path: ../../packages/uniplan_models
